@using CafeeAFM.Models;
    @model List<Wifi_Infos>


@if (Model.Count != 0)
{
    foreach (var item1 in Model)
    {

        <div class="row">
            <div class="col-6 col-sm-7">
                @*<h5 style="color:white"><i class="fa fa-check text-primary mr-3"></i>@item1.Wifi_Period</h5>*@

                @Html.TextBox("Wifi_Period", value: item1.Wifi_Period , htmlAttributes: new { @class = "h5", @readonly = "readonly" })

                @*@Html.TextBox("Bookname", value: item1.Wifi_Price, htmlAttributes: new { @class = "h5", @readonly = "readonly" })*@

                @*@Html.LabelFor(model => model.Service.Wifi_Infos.Wifi_Period, new { @class = "form-control bg-transparent border-primary p-4" })*@
                </div>
                <div class="col-6 col-sm-5">
                    <input type="number" class="form-control bg-transparent border-primary p-4 " placeholder="Quantity" required="required" />
                </div>
                </div>
                }
                }
