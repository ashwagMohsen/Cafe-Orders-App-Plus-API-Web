@using CafeeAFM.Models;
@model List<add_coffes>


@if (Model.Count != 0)
{
    foreach (var value in Model)
    {
        <div class="row">
            <div class="col-6 col-sm-7">
                @*<h5 style="color:white"><i class="fa fa-check text-primary mr-3"></i> @item1.Coffee.CoffeeName + @item1.Addition.AddType </h5>*@
                @*@Html.LabelFor(model => model.Coffee.CoffeeName, new { @class = "form-control bg-transparent border-primary p-4" })
        @Html.LabelFor(model => model.Coffee.add_coffes, new { @class = "form-control bg-transparent border-primary p-4" })*@

                @Html.TextBox("CoffeeName", value: value.Coffee.CoffeeName, htmlAttributes: new { @readonly = "readonly" })
                @Html.TextBox("AddType", value: value.Addition.AddType, htmlAttributes: new { @readonly = "readonly" })
            </div>
            <div class="col-6 col-sm-5">
                <input type="number" class="form-control bg-transparent border-primary p-4 " placeholder="Quantity" required="required" />
            </div>
        </div>
    }
}
