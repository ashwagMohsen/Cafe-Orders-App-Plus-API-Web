@using CafeeAFM.Models;
@model List<add_coffes>

@using (Html.BeginForm("MenuCoffee", "Home", FormMethod.Post))
{
        <div class="row" id="firstCoffeeRow">
            @*@if (Model.Count != 0)
            {*@
                @foreach (var value1 in Model)
                {
                    <div class="col-lg-6">
                        @*<h1 class="mb-5">@value.Coffee.CoffeeName</h1>*@

                        <div class="row align-items-center mb-5">
                            <div class="col-4 col-sm-4">
                                <img class="w-100 rounded-circle mb-3 mb-sm-0" src="~/img/menu-1.jpg" alt="">
                                <h5 class="menu-price">$5</h5>
                            </div>
                            <div class="col-6 col-sm-6">
                                @Html.TextBox("CoffeeName", value: value1.Coffee.CoffeeName, htmlAttributes: new { @readonly = "readonly" })

                                @Html.TextBox("AddType", value: value1.Addition.AddType, htmlAttributes: new { @readonly = "readonly" })
                                @Html.TextBox("Description", value: value1.Addition.Description, htmlAttributes: new { @readonly = "readonly" })
                                <button>Request</button>
                                @*<h4>@value.Addition.AddType</h4>
        <p class="m-0">@value.Addition.Description</p>*@
                            </div>
                            @*<div class="col-2 col-sm-3"></div>*@
                        </div>
                    </div>

                }
                
        </div>
}